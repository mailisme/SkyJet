name: MinecraftPvpPlugin
version: '${project.version}'
main: me.minecraft.minecraftpvpplugin.MinecraftPvpPlugin
commands:
  lobby:
    description: go back to lobby
    usage: /lobby
    aliases:
      - l
      - LOBBY
      - Lobby

  start:
    description: (OP Only) forcefully start the game
    usage: /start

  data:
    description: (OP Only) set data
    usage: /data
    aliases:
      -d